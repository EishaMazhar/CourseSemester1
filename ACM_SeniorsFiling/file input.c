#include<stdio.h>
main()
{
/*
  	int no;
	FILE *fp;
	fp=fopen("code.txt","r");    // integers input
	fscanf(fp,"%d",&no);
	printf("%d\n",no);
	int no2;
	int i=0;
	for(i=0;i<no;i++)
	{
		fscanf(fp,"%d",&no2);
		printf("%d\n",no2);	
	}
	fclose(fp);*/
	

   /* int no;
    FILE *fp;
    fp=fopen("row.txt","r"); 
    fscanf(fp,"%d",&no);
    float arr[no];                      	//row input
    int i;
    for(i=0;i<no;i++)
    {
    	fscanf(fp,"%f",&arr[i]);
		printf("%.2f\n", arr[i]);	
	}
    */
    
  /*  int r,c;
    FILE *fp;
    fp=fopen("input2.txt","r");
   	fscanf(fp,"%d",&r);
	printf("%d\n",r);
	fscanf(fp,"%d",&c);
	printf("%d\n",c);                     //matrices input
	int arr[r][c];
	int i,j;
	for(i=0;i<r;i++)
	{
		for(j=0;j<c;j++)
		{
			fscanf(fp,"%d",&arr[i][j]);
			printf("%d ",arr[i][j]);
		}
	printf("\n");
	}
	*/
	
}
